# dotnet10-nuget-mcp-fakedata
Implementação em .NET 10 (package NuGet) de MCP Server (stdio) para a geração de dados fake de empresas, contatos e produtos no padrão brasileiro. Inclui o uso da biblioteca Bogus e de um workflow do GitHub Actions para publicação do pacote no NuGet.
